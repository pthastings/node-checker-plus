{
  "name": "node-checker-plus",
  "version": "2.0.0",
  "description": "Site status monitoring dashboard - Vercel serverless edition",
  "scripts": {
    "build": "echo No build required",
    "deploy": "vercel --prod",
    "start:local": "node server/index.js"
  },
  "keywords": ["monitoring", "status", "dashboard", "vercel", "serverless", "tailscale"],
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
